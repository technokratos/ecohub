server:
  port: 8000
spring:
  application:
    name: echhub
endpoints:
  info:
    enabled: true
info:
  app:
    version: ^project.version^
    name: ^project.name^
#eureka:
#  client:
#    registerWithEureka: false
#    fetchRegistry: false